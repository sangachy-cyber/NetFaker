# 📄 NetFaker 合成数据评估报告  
**生成时间**：`2026-01-23 15:20:00`  
**真实文件**：`data/raw/campus_real.txt`  
**合成文件**：`data/outputs/sim_rule_task456.txt`  
**总时长**：`600 秒`（60 个窗口）

---

## 🔍 1. 质量概览

| 维度 | 窗口级通过率 | 序列级是否达标 | 关键风险 |
|------|--------------|----------------|--------|
| **统计分布** | ✅ 92% | — | — |
| **时序依赖** | ✅ 85% | ⚠️ `acf_mae_lag1_100 = 0.22` | 边界跳跃残留 |
| **丢包突发性** | ✅ 88% | ✅ | — |
| **状态转移** | — | ❌ `JS 散度 = 0.18` | s2→s5 跳转不足 |
| **全局稳定性** | — | ✅ | — |

> 🎯 **总体结论**：**基本可用，但需优化边界平滑与状态转移逻辑**。

---

## 📊 2. 全局可视化（核心诊断图）

![全轨迹对比](visualizations/sequence_level/full_trace_comparison.png)

> 💡 **快速解读**：
> - **延迟轨迹中的竖线 = 窗口边界** → 若有跳跃，说明平滑未生效
> - **ACF 图中 lag=10/100 虚线** → 超过此处仍高 MAE 表示跨窗口不连续
> - **状态热力图非对称？** → 检查状态跳转概率是否校准

---

## 📈 3. 核心指标详情

### 3.1 窗口级（N = 60）

| 指标 | 均值 ± std | 通过率 | 阈值 |
|------|-----------|--------|------|
| `p99_delay_err (ms)` | 182.3 ± 95.7 | 92% | ≤300 |
| `acf_mae_lag1_10` | 0.132 ± 0.041 | 85% | ≤0.15 |
| `burst_count_diff` | 0.8 ± 0.6 | 88% | ≤1 |

### 3.2 序列级

| 指标 | 值 | 是否达标 |
|------|-----|--------|
| `acf_mae_lag1_100` | 0.22 | ⚠️ |
| `state_transition_jsd` | 0.18 | ❌ |
| `rolling_p99_drift (ms)` | 62.1 | ✅ |

---

## 🚨 4. 诊断与建议（附图直达）

### 🔸 问题 1：窗口边界存在跳跃
- **证据**：
  - `acf_mae_lag1_100 = 0.22 > 0.20`
  - 全局 ACF 在 lag=10~20 区间显著偏高
- **根因定位图**：
  ![ACF跳跃诊断](visualizations/diagnosis/diagnosis_acf_jump.png)
- **建议**：
  - [ ] 检查 `BoundaryStitcher` 是否启用
  - [ ] 确认 `state_ids` 正确传递（同状态应平滑）
  - [ ] 尝试将 `overlap_points` 从 5 增至 7

---

### 🔸 问题 2：状态 s2 → s5 跳转频率偏低
- **证据**：
  - `state_transition_jsd = 0.18`
  - 热力图显示合成数据中 s2→s5 几乎缺失
- **根因定位图**：
  ![状态转移偏差](visualizations/diagnosis/diagnosis_state_bias.png)
- **建议**：
  - [ ] 重新统计真实 trace 的状态转移矩阵
  - [ ] 在 `StateSequenceGenerator` 中提升 s2→s5 跳转权重
  - [ ] 检查 s5 状态窗口池是否过小

---

## 🖼️ 5. 典型窗口样例（成功 vs 失败）

### ✅ 成功窗口 #042（ACF=0.11, burst匹配）
![Window 042](visualizations/window_level/viz_window_0042.png)

### ❌ 失败窗口 #017（ACF=0.29, 边界跳跃）
![Window 017](visualizations/window_level/viz_window_0017.png)

> 🔎 **对比可见**：失败窗口在第 100 点处出现 92ms → 50ms 骤降，未被平滑。

---

## 📁 6. 完整输出目录

所有结果位于：`data/outputs/eval/`

```
├── detailed_metrics.csv
├── sequence_metrics.json
├── evaluation_summary.md          ← 本文件
└── visualizations/
    ├── window_level/              ← 60 张窗口图
    ├── sequence_level/
    │   └── full_trace_comparison.png
    └── diagnosis/
        ├── diagnosis_acf_jump.png
        └── diagnosis_state_bias.png
```

---

> ✉️ **本报告由 NetFaker Evaluator v2.1 自动生成**  
> 修改配置后重新运行：  
> ```bash
> python scripts/evaluate_simulation.py --real ... --syn ...
> ```

---

## ✅ 设计亮点

1. **所有图直连嵌入**：使用标准 Markdown `![](path)`，无需附件或跳转  
2. **关键图置顶**：`full_trace_comparison.png` 放在第二部分，第一时间暴露全局问题  
3. **诊断图紧贴建议**：每个问题下方立即展示根因图，形成“文字+图”强关联  
4. **成功/失败样例对比**：帮助团队建立“好 vs 坏”的直观认知  
5. **路径清晰**：所有图片使用**相对路径**，报告可随输出目录整体迁移

> 💡 **使用提示**：在 VS Code 中安装 **Markdown Preview Enhanced** 插件，可获得最佳渲染效果。

---